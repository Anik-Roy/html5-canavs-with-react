export function CalcWidthHeight(prevX, prevY, mouseX, mouseY, angle, dragCode) {
    console.log(prevX, prevY, mouseX, mouseY, angle, dragCode);
    if(dragCode === 6) {
        if(angle > 0) {
            
        }
    }
}